# Python Requirements for Steel Girder Bridge Model
# Install using: pip install -r requirements.txt
# OR using conda: conda install -c conda-forge pythonocc-core

# Core dependency
pythonocc-core>=7.7.0

# Note: pythonocc-core is best installed via conda:
# conda install -c conda-forge pythonocc-core
#
# The pip installation may require additional system dependencies
# and may not work on all platforms.
